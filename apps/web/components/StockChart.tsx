"use client";
import { useEffect, useRef } from "react";
import { createChart, CandlestickSeries, HistogramSeries, type IChartApi, type UTCTimestamp } from "lightweight-charts";
import type { Candle, Tick } from "@market-watch/shared-types";

export function StockChart({ candles, tick }: { candles: Candle[]; tick?: Tick }) { const ref = useRef<HTMLDivElement>(null); const chart = useRef<IChartApi | undefined>(undefined); useEffect(() => { if (!ref.current) return; const instance = createChart(ref.current, { layout: { background: { color: "#ffffff" }, textColor: "#858092" }, grid: { vertLines: { color: "#f1eff5" }, horzLines: { color: "#f1eff5" } }, height: 390 }); const candleSeries = instance.addSeries(CandlestickSeries, { upColor: "#18a66d", downColor: "#d34b56", borderVisible: false, wickUpColor: "#18a66d", wickDownColor: "#d34b56" }); const volumeSeries = instance.addSeries(HistogramSeries, { priceFormat: { type: "volume" }, priceScaleId: "" }); const chartCandles = candles.map((candle) => ({ ...candle, time: candle.time as UTCTimestamp })); candleSeries.setData(chartCandles); volumeSeries.setData(candles.map((candle) => ({ time: candle.time as UTCTimestamp, value: candle.volume, color: candle.close >= candle.open ? "#18a66d55" : "#d34b5655" }))); if (tick && candles.length) { const last = candles[candles.length - 1]; candleSeries.update({ time: last.time as UTCTimestamp, open: last.open, high: Math.max(last.high, tick.price), low: Math.min(last.low, tick.price), close: tick.price }); } instance.timeScale().fitContent(); chart.current = instance; const observer = new ResizeObserver(() => instance.applyOptions({ width: ref.current?.clientWidth ?? 0 })); observer.observe(ref.current); return () => { observer.disconnect(); instance.remove(); chart.current = undefined; }; }, [candles, tick]); return <div ref={ref} className="chart" />; }
